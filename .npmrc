# eslint-config-next resolves Next's bundled babel parser relative to its own
# package scope. Under pnpm's isolated layout that fails unless `next` and the
# ESLint tooling are hoisted into the virtual store's shared node_modules.
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=*next*
